export const config = {
  API_URL: import.meta.env.VITE_API_URL,
  APP_NAME: import.meta.env.VITE_APP_NAME || 'Mi Aplicación',
  VERSION: import.meta.env.VITE_VERSION || '1.0.0',
};

export default config;